(function(){'use strict';const I=window.HarmoniaIdentity;let user=null;const c=document.getElementById('content');function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function setView(v){document.querySelectorAll('[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===v));document.getElementById('title').textContent=v[0].toUpperCase()+v.slice(1);if(v==='home')c.innerHTML=`<div class="h-grid"><section class="h-card"><span class="h-kicker">Welcome</span><h2>${esc(user.firstName||'Member')}</h2><p>See what is happening and get involved at your own pace.</p><div class="h-actions"><button class="h-btn primary" data-go="events">Explore events</button></div></section><section class="h-card"><span class="h-kicker">Stay connected</span><h3>Your Harmonia profile</h3><p>Keep your interests and communication preferences current.</p><button class="h-btn secondary" data-go="profile">Open profile</button></section></div>`;else if(v==='events')loadEvents();else c.innerHTML=`<section class="h-card"><span class="h-kicker">Profile</span><h2>${esc(user.firstName||'')} ${esc(user.lastName||'')}</h2><p>${esc(user.email||'')}</p><p class="h-muted">Profile editing stays intentionally simple in the community experience.</p></section>`;c.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>setView(b.dataset.go))}async function loadEvents(){c.innerHTML='<section class="h-card"><p>Loading events…</p></section>';try{const rows=await I.request('/events/upcoming');const arr=Array.isArray(rows)?rows:(rows.items||rows.data||[]);c.innerHTML=`<section class="h-card"><span class="h-kicker">Upcoming</span><h2>Gather with us</h2><div class="h-list">${arr.length?arr.slice(0,8).map(e=>`<div class="h-row"><strong>${esc(e.title)}</strong><div class="h-muted">${e.startAt?new Date(e.startAt).toLocaleString():''}</div></div>`).join(''):'<div class="h-empty">No upcoming events yet.</div>'}</div></section>`}catch(e){c.innerHTML=`<section class="h-card"><p class="h-error">${esc(e.message)}</p></section>`}}document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));document.getElementById('logout').onclick=()=>{I.clear();location.replace('/account/')};(async()=>{user=await I.guard('member',{allowPreview:true});if(!user)return;document.getElementById('welcome').textContent=`Signed in as ${user.email||''}`;setView('home')})();})();
